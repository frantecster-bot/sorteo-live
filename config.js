const CONFIG = {
  totalNumbers: 20,
  minPlayers: 2,
  drawSpeed: 120,         // ms between highlight frames during animation
  animDuration: 4000,    // ms total casino spin animation
  totalRounds: 5,         // rounds per tournament
  pointsPerWin: 100,
  pointsPerPick: 10,
  vipThreshold: 300,      // points needed for VIP badge
  hotThreshold: 3         // times a number must win to be "caliente"
};
